meta {
  name: duplicate-username
  type: http
  seq: 3
}

post {
  url: {{base_url}}/users
  body: json
  auth: none
}

body:json {
  {
    "name": "Jane Doe",
    "username": "johndoe",
    "email": "jane@example.com",
    "password": "securepass123",
    "dateOfBirth": "1992-02-02"
  }
}

tests {
  test("Status should be 409 for duplicate username", function () {
    expect(res.getStatus()).to.equal(409);
  });
  
  test("Response should indicate conflict for username", function () {
    expect(res.getBody().message).to.contain("already exists");
  });
}
