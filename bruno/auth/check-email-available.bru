meta {
  name: check-email-available
  type: http
  seq: 1
}

get {
  url: {{base_url}}/auth/check-email?email=nonexistent@example.com
  body: none
  auth: none
}

tests {
  test("Status should be 200", function () {
    expect(res.getStatus()).to.equal(200);
  });

  test("Email should be available", function () {
    expect(res.getBody().available).to.be.true;
  });
}
